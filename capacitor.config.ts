import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hamysteria.recipe',
  appName: 'RecipeApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
